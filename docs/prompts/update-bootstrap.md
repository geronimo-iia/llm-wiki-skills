# Update: bootstrap Skill

## Changes

- Tool names: `wiki_content_read` (not `wiki_read`), `wiki_content_write` (not `wiki_write`)
- Add `wiki_schema list` to session orientation (show registered types)
- Add `wiki_schema show <type> --template` for frontmatter reference
- `WikiEngine` / `EngineState` naming (if referenced)
- `--cross-wiki` flag (not `--all`) for cross-wiki search
