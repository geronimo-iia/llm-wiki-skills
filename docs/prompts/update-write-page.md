# Update: write-page Skill

## Changes

- Tool names: `wiki_content_new` (not `wiki_new_page`), `wiki_content_write` (not `wiki_write`)
- `wiki_schema show <type> --template` for frontmatter scaffolding
- `wiki_schema list` to discover available types
- Mention edge fields (`sources`, `concepts`) in the writing workflow
